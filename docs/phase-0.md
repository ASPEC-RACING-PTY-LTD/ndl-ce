# Phase 0 notes

Phase 0 is a mergeable monorepo. Operators cannot install a product
yet.

The bootstrap script at `packaging/bootstrap/get-nodal.sh` is a stub
for `curl -fsSL https://get.no-dal.com | sudo sh`. It does not install
packages or create users.
